describe('First', () => {
  it('deve ser possivel somar 2 números', () => {
    expect(2 + 2).toBe(4);
  });
  it('deve ser possivel somar 2 números', () => {
    expect(2 + 2).not.toBe(5);
  });
});
